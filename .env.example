# Node Environment (production or development)
NODE_ENV="production"

# Port for the server (defaults to 3000)
PORT="3000"

# Optional GEMINI_API_KEY if AI features are enabled
GEMINI_API_KEY=""

# Optional APP_URL if hosted on custom domain
APP_URL="http://localhost:3000"

